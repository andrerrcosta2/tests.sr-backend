// const path = require('path');

// class CustomReporter {
//     onTestStart(test) {
//         Terminal.title(`START: ${test.name}`);
//     }

//     onTestResult(test, testResult) {
//         Terminal.title(`END: ${test.name} `, testResult.status);
//     }
// }


// module.exports = CustomReporter;
